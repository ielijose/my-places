import { createAction } from 'redux-actions';

export const addPlace = createAction('addPlace');
export const removePlace = createAction('removePlace');

export const setUserLocation = createAction('setUserLocation');
